// deprecated
var name = 'Joe';

// expect value to change
let name = 'Joe';

// expect value NOT to change
const name = 'Joe';

// expect memory pointer not to change
// but values within may change
const arr = [ 1, 2, 3 ];
const obj = { name: 'Joe' };
